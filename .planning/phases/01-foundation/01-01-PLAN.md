---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Python project infrastructure with all dependencies and package structure.

Purpose: Establish the foundation that all subsequent phases build upon.
Output: Installable Python package with development dependencies.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Project constraints:**
- Python 3.11+
- macOS only
- SQLite database at ~/.drivecatalog/catalog.db
- CLI via Click, output via Rich
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with project configuration</name>
  <files>pyproject.toml</files>
  <action>
Create pyproject.toml using modern Python packaging (PEP 621):

**Project metadata:**
- name: drivecatalog
- version: 0.1.0
- description: CLI tool for cataloging external drives and detecting duplicates
- requires-python: ">=3.11"

**Dependencies:**
- click>=8.1.0 (CLI framework)
- rich>=13.0.0 (terminal output)
- xxhash>=3.0.0 (fast hashing for duplicate detection)
- watchdog>=3.0.0 (filesystem monitoring)
- ffmpeg-python>=0.2.0 (media metadata extraction)

**Dev dependencies (optional group):**
- pytest>=7.0.0
- pytest-cov>=4.0.0
- ruff>=0.1.0 (linting)

**Entry point:**
- drivecatalog = "drivecatalog.cli:main"

**Build system:**
- Use setuptools with pyproject.toml backend

Do NOT add complex build configurations. Keep it minimal and standard.
  </action>
  <verify>python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))" succeeds (valid TOML)</verify>
  <done>pyproject.toml exists, valid TOML, has all required dependencies listed</done>
</task>

<task type="auto">
  <name>Task 2: Create package structure with entry points</name>
  <files>src/drivecatalog/__init__.py, src/drivecatalog/__main__.py, src/drivecatalog/cli.py</files>
  <action>
Create src-layout package structure:

**src/drivecatalog/__init__.py:**
- Package version: __version__ = "0.1.0"
- Empty otherwise (no premature abstractions)

**src/drivecatalog/__main__.py:**
- Allow `python -m drivecatalog` execution
- Import and call main from cli module

**src/drivecatalog/cli.py:**
- Import click
- Create placeholder `main` function with @click.group() decorator
- Add --version option using package version
- Single line docstring for CLI help

Do NOT create empty placeholder modules for future features. Only create what's needed now.
  </action>
  <verify>pip install -e . && drivecatalog --version outputs "0.1.0"</verify>
  <done>Package installs in editable mode, `drivecatalog --version` works, `python -m drivecatalog` works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e .` succeeds without errors
- [ ] `drivecatalog --help` shows CLI help
- [ ] `drivecatalog --version` shows 0.1.0
- [ ] `python -m drivecatalog --help` works
- [ ] No import errors
</verification>

<success_criteria>

- pyproject.toml valid and complete
- Package structure follows src-layout
- CLI entry point functional
- Ready for database module (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
