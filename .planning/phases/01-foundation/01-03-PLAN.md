---
phase: 01-foundation
plan: 03
type: execute
---

<objective>
Create Click CLI with command structure and database initialization on startup.

Purpose: Establish the user-facing interface that all commands will use.
Output: Working CLI with help, version, and database init on first run.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/drivecatalog/cli.py
@src/drivecatalog/database.py

**CLI requirements from PROJECT.md:**
- Click framework
- Commands: drives list, drives scan, search, duplicates, analyze
- (Commands implemented in later phases, structure now)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand CLI with command group structure</name>
  <files>src/drivecatalog/cli.py</files>
  <action>
Expand CLI module with proper Click structure:

**Main group (@click.group()):**
- Add callback that runs init_db() to ensure database exists
- Use `invoke_without_command=True` to show help when no subcommand

**drives subgroup (@main.group()):**
- Placeholder group for drive-related commands
- Will contain: list, add, scan, remove (implemented later)

**Placeholder commands (empty for now):**
- `@drives.command()` for `list` - just pass
- Other commands added in Phase 2

**Version option:**
- Already exists, keep it
- Use `from drivecatalog import __version__`

**Help text:**
- Main: "DriveCatalog - Catalog external drives and detect duplicates"
- drives: "Manage drive registrations"

Do NOT implement actual command logic - just structure.
Do NOT add all commands now - only `drives` group with `list` placeholder.
  </action>
  <verify>drivecatalog --help shows main help; drivecatalog drives --help shows drives subgroup</verify>
  <done>CLI has main group, drives subgroup, list placeholder, help text displays correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add status command showing database info</name>
  <files>src/drivecatalog/cli.py</files>
  <action>
Add a `status` command to verify setup works:

**@main.command() status:**
- Show database path
- Show if database exists
- Show table counts (drives, files)
- Use click.echo for output (Rich integration in Plan 04)

Example output:
```
Database: /Users/name/.drivecatalog/catalog.db
Status: initialized
Drives: 0
Files: 0
```

This provides immediate verification that everything works.

Do NOT add complex formatting yet - plain text is fine.
Do NOT make it verbose - keep it simple for foundation.
  </action>
  <verify>drivecatalog status shows database path and table counts</verify>
  <done>status command works, shows database location, shows drive/file counts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `drivecatalog --help` shows all commands
- [ ] `drivecatalog --version` shows version
- [ ] `drivecatalog drives --help` shows drives subgroup
- [ ] `drivecatalog drives list` runs (empty output is fine)
- [ ] `drivecatalog status` shows database info
- [ ] Database is auto-created on first CLI invocation
</verification>

<success_criteria>

- CLI structure established with Click
- Database initialized automatically on startup
- status command provides verification
- Ready for Rich integration (Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
