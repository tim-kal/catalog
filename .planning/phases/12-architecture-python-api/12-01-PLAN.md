---
phase: 12-architecture-python-api
plan: 01
type: execute
domain: python-api
---

<objective>
Set up FastAPI application structure with Pydantic response models for the DriveCatalog API.

Purpose: Establish the foundation for the HTTP API layer that will expose existing CLI functionality to the SwiftUI frontend.
Output: Working FastAPI app with health endpoint, Pydantic models for all response types, and uvicorn runner.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-architecture-python-api/12-RESEARCH.md

# Key existing modules to wrap:
@src/drivecatalog/database.py
@src/drivecatalog/drives.py
@src/drivecatalog/scanner.py
@src/drivecatalog/duplicates.py
@src/drivecatalog/search.py

**Tech stack to add:**
- FastAPI 0.115+ (HTTP API framework)
- uvicorn 0.32+ (ASGI server)
- Pydantic 2.x (included with FastAPI)

**Architecture from research:**
- Thin API layer wrapping existing domain modules
- Local HTTP on 127.0.0.1:8100
- CORS middleware for localhost access
- Lifespan context for database initialization
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API directory structure and dependencies</name>
  <files>src/drivecatalog/api/__init__.py, src/drivecatalog/api/main.py, src/drivecatalog/api/__main__.py, pyproject.toml</files>
  <action>
  1. Create src/drivecatalog/api/ directory with __init__.py (empty or with version)
  2. Update pyproject.toml to add FastAPI dependencies:
     - Add "fastapi[standard]>=0.115.0" to dependencies
     - This includes uvicorn, pydantic, etc.
  3. Create __main__.py entry point:
     - Parse --port (default 8100) and --host (default 127.0.0.1) arguments
     - Call uvicorn.run() with drivecatalog.api.main:app
  4. Create main.py with basic FastAPI app:
     - Import FastAPI, CORSMiddleware
     - Create app with title="DriveCatalog API", version from package
     - Add CORSMiddleware allowing all origins (local desktop app)
     - Add lifespan context manager that calls init_db() on startup
     - Add /health endpoint returning {"status": "ok"}

  Use lifespan context manager pattern (not deprecated @app.on_event):
  ```python
  @asynccontextmanager
  async def lifespan(app: FastAPI):
      init_db()
      yield
  ```
  </action>
  <verify>
  cd /Users/timkalkkuhl/code/DriveSnapshots && pip install -e . && python -m drivecatalog.api --port 8100 &
  sleep 2 && curl -s http://127.0.0.1:8100/health && kill %1
  </verify>
  <done>API starts on port 8100, /health returns {"status":"ok"}, no errors on startup</done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic response models</name>
  <files>src/drivecatalog/api/models/__init__.py, src/drivecatalog/api/models/drive.py, src/drivecatalog/api/models/file.py, src/drivecatalog/api/models/scan.py</files>
  <action>
  Create src/drivecatalog/api/models/ directory with Pydantic v2 response models.

  drive.py:
  - DriveResponse: id (int), name (str), uuid (str|None), mount_path (str), total_bytes (int), last_scan (datetime|None), file_count (int)
  - DriveListResponse: drives (list[DriveResponse]), total (int)
  - DriveCreateRequest: path (str), name (str|None = None)
  - DriveStatusResponse: id, name, mounted (bool), file_count, hashed_count, hash_coverage_percent (float)

  file.py:
  - FileResponse: id (int), drive_id (int), drive_name (str), path (str), filename (str), size_bytes (int), mtime (str|None), partial_hash (str|None), is_media (bool)
  - FileListResponse: files (list[FileResponse]), total (int), page (int), page_size (int)
  - DuplicateFile: drive_name (str), path (str), file_id (int)
  - DuplicateCluster: partial_hash (str), size_bytes (int), count (int), reclaimable_bytes (int), files (list[DuplicateFile])
  - DuplicateStatsResponse: total_clusters (int), total_duplicate_files (int), total_bytes (int), reclaimable_bytes (int)
  - DuplicateListResponse: clusters (list[DuplicateCluster]), stats (DuplicateStatsResponse)
  - SearchResultResponse: files (list[FileResponse]), total (int), pattern (str)

  scan.py:
  - ScanResultResponse: new_files (int), modified_files (int), unchanged_files (int), errors (int), total_scanned (int)
  - OperationResponse: id (str), type (str - scan/hash/copy/media/verify), status (str - pending/running/completed/failed), progress_percent (float|None), result (dict|None), error (str|None), created_at (datetime), completed_at (datetime|None)
  - CopyRequest: source_drive (str), source_path (str), dest_drive (str), dest_path (str|None = None)
  - CopyResultResponse: bytes_copied (int), source_hash (str), dest_hash (str), verified (bool)
  - MediaMetadataResponse: file_id (int), duration_seconds (float|None), codec_name (str|None), width (int|None), height (int|None), frame_rate (str|None), bit_rate (int|None), integrity_verified_at (datetime|None), integrity_errors (str|None)

  All models should use:
  - model_config = ConfigDict(from_attributes=True) for ORM-style creation from sqlite3.Row
  - Proper type hints with | None for optional fields

  __init__.py should re-export all models for easy importing.
  </action>
  <verify>python -c "from drivecatalog.api.models import DriveResponse, FileResponse, ScanResultResponse; print('Models imported successfully')"</verify>
  <done>All Pydantic models defined and importable, no validation errors</done>
</task>

<task type="auto">
  <name>Task 3: Create routes directory structure and status endpoint</name>
  <files>src/drivecatalog/api/routes/__init__.py, src/drivecatalog/api/routes/status.py, src/drivecatalog/api/main.py</files>
  <action>
  1. Create src/drivecatalog/api/routes/ directory with empty __init__.py

  2. Create routes/status.py with GET /status endpoint:
     - Import APIRouter from fastapi
     - Create router = APIRouter(tags=["status"])
     - Implement GET /status that returns database statistics:
       - Database path
       - drives_count
       - files_count
       - hashed_count
       - hash_coverage_percent
     - Use existing get_db_path() and get_connection() from database module
     - Follow pattern of cli.py status command

  3. Update main.py to include status router:
     - from .routes import status
     - app.include_router(status.router)

  The route should be a thin wrapper around database queries, NOT duplicating CLI logic.
  </action>
  <verify>
  cd /Users/timkalkkuhl/code/DriveSnapshots && python -m drivecatalog.api --port 8100 &
  sleep 2 && curl -s http://127.0.0.1:8100/status | python -m json.tool && kill %1
  </verify>
  <done>GET /status returns JSON with db_path, drives_count, files_count, hashed_count, hash_coverage_percent</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e .` installs without errors (FastAPI dependencies resolved)
- [ ] `python -m drivecatalog.api --port 8100` starts server
- [ ] `curl http://127.0.0.1:8100/health` returns {"status": "ok"}
- [ ] `curl http://127.0.0.1:8100/status` returns database statistics JSON
- [ ] All Pydantic models importable from drivecatalog.api.models
- [ ] No Python syntax errors or import failures
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- FastAPI app runs without errors
- Pydantic models ready for use in subsequent route implementations
</success_criteria>

<output>
After completion, create `.planning/phases/12-architecture-python-api/12-01-SUMMARY.md`
</output>
