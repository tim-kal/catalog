---
phase: 12-architecture-python-api
plan: 02
type: execute
domain: python-api
---

<objective>
Implement drives API routes for CRUD operations and drive status queries.

Purpose: Enable the SwiftUI frontend to manage drive registrations and view drive status.
Output: Working /drives endpoints for listing, adding, removing, and checking drive status.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-architecture-python-api/12-RESEARCH.md
@.planning/phases/12-architecture-python-api/12-01-SUMMARY.md

# Existing modules to wrap:
@src/drivecatalog/drives.py
@src/drivecatalog/database.py
@src/drivecatalog/cli.py (lines 45-126 for add/list patterns)

# API foundation from previous plan:
@src/drivecatalog/api/main.py
@src/drivecatalog/api/models/drive.py

**Established patterns:**
- Use get_connection() for database access
- Close connections in finally blocks
- Use get_drive_info() and validate_mount_path() from drives module
- Return Pydantic response models
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GET /drives and POST /drives endpoints</name>
  <files>src/drivecatalog/api/routes/drives.py, src/drivecatalog/api/main.py</files>
  <action>
  Create routes/drives.py with drives CRUD routes:

  1. Create router = APIRouter(prefix="/drives", tags=["drives"])

  2. GET /drives - List all registered drives:
     - Query drives table with file count
     - Use same SQL pattern as cli.py list_drives()
     - Return DriveListResponse with list of DriveResponse

  3. POST /drives - Register a new drive:
     - Accept DriveCreateRequest (path, optional name)
     - Validate path with validate_mount_path()
     - Get drive info with get_drive_info()
     - Check for existing registration (by UUID or mount_path)
     - Insert into database
     - Return DriveResponse with created drive
     - Raise HTTPException(400) for invalid path or already registered
     - Raise HTTPException(404) if path doesn't exist

  Update main.py to include drives router:
  - from .routes import drives
  - app.include_router(drives.router)

  Use same database logic as CLI but return JSON instead of Rich tables.
  </action>
  <verify>
  # Start server and test endpoints
  cd /Users/timkalkkuhl/code/DriveSnapshots && python -m drivecatalog.api --port 8100 &
  sleep 2
  curl -s http://127.0.0.1:8100/drives | python -m json.tool
  kill %1
  </verify>
  <done>GET /drives returns list of drives, POST /drives adds new drive with validation</done>
</task>

<task type="auto">
  <name>Task 2: Implement DELETE /drives/{name} endpoint</name>
  <files>src/drivecatalog/api/routes/drives.py</files>
  <action>
  Add DELETE endpoint to routes/drives.py:

  DELETE /drives/{name} - Remove a drive registration:
  - Accept drive name as path parameter
  - Look up drive by name
  - Raise HTTPException(404) if drive not found
  - Delete associated files first (foreign key)
  - Delete drive from drives table
  - Return {"status": "deleted", "name": name, "files_removed": count}

  NOTE: This is a destructive operation. The CLI doesn't have this but the UI will need it.
  Add a confirmation query parameter ?confirm=true that must be set:
  - If confirm is not true, return 400 with message about confirmation required
  - This prevents accidental deletions from the API

  Be careful with SQL DELETE ordering due to foreign keys (files first, then drive).
  </action>
  <verify>
  # Test that delete requires confirmation
  cd /Users/timkalkkuhl/code/DriveSnapshots && python -m drivecatalog.api --port 8100 &
  sleep 2
  curl -s -X DELETE "http://127.0.0.1:8100/drives/TestDrive" | python -m json.tool  # Should fail without confirm
  kill %1
  </verify>
  <done>DELETE /drives/{name} removes drive with confirmation required, proper 404 handling</done>
</task>

<task type="auto">
  <name>Task 3: Implement GET /drives/{name} and GET /drives/{name}/status endpoints</name>
  <files>src/drivecatalog/api/routes/drives.py</files>
  <action>
  Add detail and status endpoints to routes/drives.py:

  1. GET /drives/{name} - Get single drive details:
     - Look up drive by name with file count
     - Raise HTTPException(404) if not found
     - Return DriveResponse

  2. GET /drives/{name}/status - Get drive status with hash coverage:
     - Look up drive by name
     - Check if mount_path exists (mounted status)
     - Query file count and hashed count
     - Calculate hash coverage percentage
     - Return DriveStatusResponse:
       - id, name, mounted (bool), file_count, hashed_count, hash_coverage_percent
       - last_scan timestamp
       - media_count (files with is_media=1)

  These endpoints provide detailed drive information for the UI drive management view.
  </action>
  <verify>
  cd /Users/timkalkkuhl/code/DriveSnapshots && python -m drivecatalog.api --port 8100 &
  sleep 2
  # List drives to get a name, then query status
  curl -s http://127.0.0.1:8100/drives | python -m json.tool
  curl -s http://127.0.0.1:8100/drives/SomeDrive/status 2>/dev/null | python -m json.tool || echo "No drives or drive not found (expected if empty)"
  kill %1
  </verify>
  <done>GET /drives/{name} returns drive details, GET /drives/{name}/status returns mounted status and hash coverage</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m drivecatalog.api --port 8100` starts server
- [ ] `GET /drives` returns list of registered drives
- [ ] `POST /drives` with valid path creates new drive registration
- [ ] `POST /drives` with invalid path returns 400 error
- [ ] `DELETE /drives/{name}` requires confirmation parameter
- [ ] `GET /drives/{name}` returns single drive details
- [ ] `GET /drives/{name}/status` returns mounted status and statistics
- [ ] All endpoints return proper JSON with Pydantic models
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Drives CRUD operations work end-to-end
- Proper HTTP error codes for validation failures
</success_criteria>

<output>
After completion, create `.planning/phases/12-architecture-python-api/12-02-SUMMARY.md`
</output>
