---
phase: 13-swiftui-project-setup
plan: 02
type: execute
---

<objective>
Create navigation shell with sidebar for DriveCatalog macOS app.

Purpose: Establish the primary navigation structure that organizes access to drives, files, duplicates, and search functionality.
Output: Working NavigationSplitView with sidebar menu and placeholder content areas.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-swiftui-project-setup/13-01-SUMMARY.md

**Prior plan context:**
- 13-01 complete: Xcode project with basic SwiftUI app structure
- DriveCatalogApp.swift and ContentView.swift exist

**Navigation structure based on upcoming phases:**
- Drives (Phase 15) - Drive management, scan status
- Browser (Phase 16) - File tree/list browsing
- Duplicates (Phase 17) - Duplicate clusters, reclaimable space
- Search (Phase 18) - Cross-drive file search
- Settings (Phase 20) - App preferences, mount automation

**SwiftUI patterns:**
- NavigationSplitView for macOS 14+ three-column layout
- @State for selection binding
- SF Symbols for sidebar icons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation model and sidebar view</name>
  <files>DriveCatalog/Navigation/NavigationModel.swift, DriveCatalog/Navigation/Sidebar.swift</files>
  <action>
Create DriveCatalog/Navigation/ directory.

Create NavigationModel.swift with:
- SidebarItem enum: drives, browser, duplicates, search, settings
- Each case has title (String) and systemImage (String, SF Symbol name)
- CaseIterable conformance for iteration
- Identifiable conformance (id = self)

Create Sidebar.swift with:
- Sidebar view that shows List of SidebarItem.allCases
- Each item: Label(item.title, systemImage: item.systemImage)
- Selection binding to pass to parent

Use SF Symbols:
- drives: "externaldrive"
- browser: "folder"
- duplicates: "doc.on.doc"
- search: "magnifyingglass"
- settings: "gear"
  </action>
  <verify>swift build (if SPM) or xcodebuild -scheme DriveCatalog build</verify>
  <done>NavigationModel.swift and Sidebar.swift compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder content views and wire up NavigationSplitView</name>
  <files>DriveCatalog/Views/DrivesView.swift, DriveCatalog/Views/BrowserView.swift, DriveCatalog/Views/DuplicatesView.swift, DriveCatalog/Views/SearchView.swift, DriveCatalog/Views/SettingsView.swift, DriveCatalog/ContentView.swift</files>
  <action>
Create DriveCatalog/Views/ directory.

Create placeholder views (each ~10 lines):
- DrivesView.swift: "Drives" title, Text("Drive management coming in Phase 15")
- BrowserView.swift: "Browser" title, Text("File browser coming in Phase 16")
- DuplicatesView.swift: "Duplicates" title, Text("Duplicate detection coming in Phase 17")
- SearchView.swift: "Search" title, Text("Search interface coming in Phase 18")
- SettingsView.swift: "Settings" title, Text("Settings coming in Phase 20")

Each placeholder should have:
- VStack with .navigationTitle()
- Spacer() to push content up
- Centered placeholder text

Update ContentView.swift:
- @State private var selection: SidebarItem? = .drives
- NavigationSplitView with sidebar, detail columns
- Sidebar: Sidebar(selection: $selection)
- Detail: switch on selection to show appropriate view
- Default selection to .drives on appear

macOS window sizing hint: .frame(minWidth: 800, minHeight: 500)
  </action>
  <verify>xcodebuild -scheme DriveCatalog build succeeds without warnings</verify>
  <done>NavigationSplitView renders with sidebar and correct detail view for each selection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Navigation shell with sidebar and placeholder content views</what-built>
  <how-to-verify>
    1. Run: Cmd+R in Xcode (or open DriveCatalog.xcodeproj and run)
    2. Verify: Sidebar appears on left with 5 items (Drives, Browser, Duplicates, Search, Settings)
    3. Click each sidebar item:
       - Drives shows "Drive management coming in Phase 15"
       - Browser shows "File browser coming in Phase 16"
       - Duplicates shows "Duplicate detection coming in Phase 17"
       - Search shows "Search interface coming in Phase 18"
       - Settings shows "Settings coming in Phase 20"
    4. Confirm: Each view has appropriate navigation title
    5. Check: Window has reasonable minimum size (~800x500)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 13, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] DriveCatalog/Navigation/ contains NavigationModel.swift and Sidebar.swift
- [ ] DriveCatalog/Views/ contains 5 placeholder views
- [ ] ContentView.swift uses NavigationSplitView
- [ ] xcodebuild -scheme DriveCatalog build succeeds
- [ ] Sidebar navigation works for all 5 items
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Navigation shell renders correctly
- Sidebar selection changes detail view
- Phase 13 complete, ready for Phase 14 (Swift Data Models)
  </success_criteria>

<output>
After completion, create `.planning/phases/13-swiftui-project-setup/13-02-SUMMARY.md`:

# Phase 13 Plan 02: Navigation Shell Summary

**[One-liner: what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `DriveCatalog/Navigation/NavigationModel.swift` - Sidebar item enum
- `DriveCatalog/Navigation/Sidebar.swift` - Sidebar view
- `DriveCatalog/Views/DrivesView.swift` - Placeholder
- `DriveCatalog/Views/BrowserView.swift` - Placeholder
- `DriveCatalog/Views/DuplicatesView.swift` - Placeholder
- `DriveCatalog/Views/SearchView.swift` - Placeholder
- `DriveCatalog/Views/SettingsView.swift` - Placeholder
- `DriveCatalog/ContentView.swift` - Updated with NavigationSplitView

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 13 complete, ready for Phase 14: Swift Data Models
</output>
