---
phase: 14-swift-data-models
plan: 01
type: execute
---

<objective>
Define Swift types for drives, files, duplicates, and operations matching Python API response models.

Purpose: Enable the SwiftUI app to decode JSON responses from the FastAPI backend with type-safe Swift structs.
Output: Codable Swift structs in DriveCatalog/Models/ mirroring all Pydantic response models.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/12-architecture-python-api/12-01-SUMMARY.md
@.planning/phases/13-swiftui-project-setup/13-02-SUMMARY.md

# Python API models to mirror:
@src/drivecatalog/api/models/drive.py
@src/drivecatalog/api/models/file.py
@src/drivecatalog/api/models/scan.py

**Tech stack available:** FastAPI, Pydantic, SwiftUI, NavigationSplitView
**Established patterns:** Pydantic models with from_attributes=True, SwiftUI placeholder views
**Constraining decisions:**
- Phase 12: API returns JSON with snake_case keys (Pydantic default)
- Phase 13: macOS 14.0 deployment target
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Drive models</name>
  <files>DriveCatalog/Models/Drive.swift</files>
  <action>
Create Codable structs matching Python drive models:
- DriveResponse: id (Int), name (String), uuid (String?), mountPath (String), totalBytes (Int64), lastScan (Date?), fileCount (Int)
- DriveListResponse: drives ([DriveResponse]), total (Int)
- DriveCreateRequest: path (String), name (String?)
- DriveStatusResponse: id (Int), name (String), mounted (Bool), fileCount (Int), hashedCount (Int), hashCoveragePercent (Double), lastScan (Date?), mediaCount (Int)

Use CodingKeys to map snake_case JSON keys to camelCase Swift properties (e.g., mount_path -> mountPath).
Use Int64 for byte counts to match Python int capacity.
Configure JSONDecoder with .iso8601 date strategy for datetime fields.
  </action>
  <verify>swift build compiles without errors</verify>
  <done>Drive.swift contains 4 Codable structs with CodingKeys for snake_case mapping</done>
</task>

<task type="auto">
  <name>Task 2: Create File, Duplicate, and Search models</name>
  <files>DriveCatalog/Models/File.swift</files>
  <action>
Create Codable structs matching Python file models:
- FileResponse: id (Int), driveId (Int), driveName (String), path (String), filename (String), sizeBytes (Int64), mtime (String?), partialHash (String?), isMedia (Bool)
- FileListResponse: files ([FileResponse]), total (Int), page (Int), pageSize (Int)
- DuplicateFile: driveName (String), path (String), fileId (Int)
- DuplicateCluster: partialHash (String), sizeBytes (Int64), count (Int), reclaimableBytes (Int64), files ([DuplicateFile])
- DuplicateStatsResponse: totalClusters (Int), totalDuplicateFiles (Int), totalBytes (Int64), reclaimableBytes (Int64)
- DuplicateListResponse: clusters ([DuplicateCluster]), stats (DuplicateStatsResponse)
- SearchFile: driveName (String), path (String), sizeBytes (Int64), mtime (String?)
- SearchResultResponse: files ([SearchFile]), total (Int), pattern (String)

Use CodingKeys for snake_case -> camelCase mapping throughout.
  </action>
  <verify>swift build compiles without errors</verify>
  <done>File.swift contains 8 Codable structs covering files, duplicates, and search responses</done>
</task>

<task type="auto">
  <name>Task 3: Create Operation models</name>
  <files>DriveCatalog/Models/Operation.swift</files>
  <action>
Create Codable structs matching Python operation models:
- ScanResultResponse: newFiles (Int), modifiedFiles (Int), unchangedFiles (Int), errors (Int), totalScanned (Int)
- OperationResponse: id (String), type (String), status (String), progressPercent (Double?), result ([String: AnyCodable]?), error (String?), createdAt (Date), completedAt (Date?)
- CopyRequest: sourceDrive (String), sourcePath (String), destDrive (String), destPath (String?)
- CopyResultResponse: bytesCopied (Int64), sourceHash (String), destHash (String), verified (Bool)
- MediaMetadataResponse: fileId (Int), durationSeconds (Double?), codecName (String?), width (Int?), height (Int?), frameRate (String?), bitRate (Int?), integrityVerifiedAt (Date?), integrityErrors (String?)

For OperationResponse.result (dict[str, Any]), use a simple approach: either decode as [String: String] for now, or skip the field if complex nested types aren't needed yet. The simpler approach is preferred - the UI can request specific endpoints rather than parsing arbitrary dict results.

Use CodingKeys for snake_case -> camelCase mapping.
  </action>
  <verify>swift build compiles without errors</verify>
  <done>Operation.swift contains 5 Codable structs for scan, copy, and operation tracking</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `swift build` succeeds without errors
- [ ] All 17 Codable structs defined across 3 files
- [ ] CodingKeys properly map snake_case to camelCase for all structs
- [ ] Int64 used for byte count fields
- [ ] Optional types match nullable Python fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No compiler errors or warnings
- Swift types match Python Pydantic model fields exactly
</success_criteria>

<output>
After completion, create `.planning/phases/14-swift-data-models/14-01-SUMMARY.md`
</output>
