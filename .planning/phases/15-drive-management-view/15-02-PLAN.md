---
phase: 15-drive-management-view
plan: 02
type: execute
---

<objective>
Build the drive list view with add/remove functionality.

Purpose: Let users see all registered drives and manage registration.
Output: DriveListView showing all drives with add sheet and delete confirmation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan in this phase:
@.planning/phases/15-drive-management-view/15-01-SUMMARY.md

# API service to use:
@DriveCatalog/Services/APIService.swift

# Models:
@DriveCatalog/Models/Drive.swift

# Existing view to replace:
@DriveCatalog/Views/DrivesView.swift

**Established patterns:**
- NavigationSplitView with sidebar selection (Phase 13-02)
- SF Symbols for consistent macOS iconography
- @State for local view state, async/await for data loading
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DriveListView with loading states</name>
  <files>DriveCatalog/Views/Drives/DriveListView.swift</files>
  <action>
Create DriveListView as a List-based view showing all registered drives.

Structure:
- @State drives: [DriveResponse] = []
- @State isLoading = true
- @State errorMessage: String? = nil
- @State showAddSheet = false

View body:
- If isLoading: ProgressView with "Loading drives..."
- If errorMessage: Error display with retry button
- If drives.isEmpty: Empty state with "No drives registered" and add button
- Otherwise: List of DriveRow items

DriveRow (inline or separate struct):
- Drive name as primary text (bold)
- Mount path as secondary text (smaller, gray)
- File count badge (e.g., "1,234 files")
- Last scanned date (relative: "2 hours ago" or "Never")
- Green/red circle for mounted status (use API status endpoint or local check)

Toolbar:
- Add button (plus icon) that sets showAddSheet = true
- Refresh button

Load drives in .task {} using APIService.shared.fetchDrives()
Handle errors gracefully - display message but don't crash.
  </action>
  <verify>xcodebuild build succeeds, DriveListView.swift compiles</verify>
  <done>DriveListView shows list of drives with loading/empty/error states</done>
</task>

<task type="auto">
  <name>Task 2: Create AddDriveSheet and wire up deletion</name>
  <files>DriveCatalog/Views/Drives/AddDriveSheet.swift, DriveCatalog/Views/Drives/DriveListView.swift</files>
  <action>
Create AddDriveSheet as a sheet for registering new drives.

AddDriveSheet structure:
- @State path: String = ""
- @State customName: String = ""
- @State isSubmitting = false
- @State errorMessage: String? = nil
- @Environment(\.dismiss) var dismiss

View body:
- Form with:
  - TextField for path (placeholder: "/Volumes/MyDrive")
  - Optional TextField for custom name
  - If errorMessage: Text showing error in red
- Toolbar with Cancel and Add buttons
- Add button disabled if path.isEmpty or isSubmitting
- On submit: Call APIService.shared.createDrive(), dismiss on success, show error on failure

In DriveListView:
- Add .sheet(isPresented: $showAddSheet) { AddDriveSheet(onAdded: loadDrives) }
- Add swipe action or context menu to delete drives
- Delete calls APIService.shared.deleteDrive(name:)
- Show confirmation alert before deleting (destructive action)
- Refresh list after add/delete

Update DrivesView.swift:
- Replace placeholder with DriveListView()
  </action>
  <verify>xcodebuild build succeeds</verify>
  <done>AddDriveSheet works, delete with confirmation works, DrivesView shows DriveListView</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project DriveCatalog.xcodeproj -scheme DriveCatalog build` succeeds
- [ ] DriveListView.swift exists in DriveCatalog/Views/Drives/
- [ ] AddDriveSheet.swift exists in DriveCatalog/Views/Drives/
- [ ] DrivesView.swift updated to show DriveListView
- [ ] No compiler warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Drive list displays with proper states
- Add and delete drives work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/15-drive-management-view/15-02-SUMMARY.md`
</output>
