---
phase: 23-migration-wizard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DriveCatalog/Models/Consolidation.swift
  - DriveCatalog/Models/Migration.swift
  - DriveCatalog/Services/APIService.swift
autonomous: true

must_haves:
  truths:
    - "Swift can decode all consolidation API responses (distribution, candidates, strategy)"
    - "Swift can decode all migration API responses (plan summary, plan details, validation, files, execute)"
    - "APIService has async methods for every consolidation and migration endpoint"
  artifacts:
    - path: "DriveCatalog/Models/Consolidation.swift"
      provides: "Codable structs for consolidation API responses"
      contains: "DriveDistributionResponse"
    - path: "DriveCatalog/Models/Migration.swift"
      provides: "Codable structs for migration API responses"
      contains: "MigrationPlanResponse"
    - path: "DriveCatalog/Services/APIService.swift"
      provides: "Async methods for consolidation and migration endpoints"
      contains: "fetchConsolidationCandidates"
  key_links:
    - from: "DriveCatalog/Models/Consolidation.swift"
      to: "GET /consolidation/candidates"
      via: "CodingKeys mapping snake_case JSON to camelCase Swift"
      pattern: "ConsolidationCandidatesResponse.*Codable"
    - from: "DriveCatalog/Models/Migration.swift"
      to: "GET /migrations/{id}"
      via: "CodingKeys mapping snake_case JSON to camelCase Swift"
      pattern: "MigrationPlanResponse.*Codable"
    - from: "DriveCatalog/Services/APIService.swift"
      to: "DriveCatalog/Models/Consolidation.swift"
      via: "return type of fetchConsolidationCandidates"
      pattern: "func fetchConsolidationCandidates.*ConsolidationCandidatesResponse"
---

<objective>
Create Codable Swift structs mirroring all consolidation and migration Pydantic API models, plus APIService methods for every consolidation/migration endpoint.

Purpose: The wizard UI (Plan 23-02) needs type-safe Swift models and networking methods to consume the backend APIs built in Phases 21-22.
Output: Consolidation.swift, Migration.swift model files + APIService extensions for 11 endpoints
</objective>

<execution_context>
@/Users/timkalkkuhl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/timkalkkuhl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Backend API models (source of truth for Swift structs)
@src/drivecatalog/api/models/consolidation.py
@src/drivecatalog/api/models/migration.py

# Backend API routes (source of truth for endpoint paths and parameters)
@src/drivecatalog/api/routes/consolidation.py
@src/drivecatalog/api/routes/migrations.py

# Existing Swift patterns to follow
@DriveCatalog/Models/Drive.swift
@DriveCatalog/Models/Operation.swift
@DriveCatalog/Services/APIService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidation and Migration Swift Models</name>
  <files>
    DriveCatalog/Models/Consolidation.swift
    DriveCatalog/Models/Migration.swift
  </files>
  <action>
Create two new Swift model files mirroring the Python Pydantic models exactly.

**Consolidation.swift** -- mirror `src/drivecatalog/api/models/consolidation.py`:

1. `DriveDistribution: Codable, Identifiable` -- fields: driveId (Int), driveName (String), totalFiles (Int), totalSizeBytes (Int64), uniqueFiles (Int), uniqueSizeBytes (Int64), duplicatedFiles (Int), duplicatedSizeBytes (Int64), reclaimableBytes (Int64), totalBytes (Int64?), usedBytes (Int64?), freeBytes (Int64?). CodingKeys for snake_case. Computed `id` returning `driveId`.

2. `DriveDistributionResponse: Codable` -- fields: drives ([DriveDistribution]), totalDrives (Int). CodingKeys: total_drives.

3. `ConsolidationTargetDrive: Codable` -- fields: driveName (String), freeBytes (Int64). CodingKeys for snake_case.

4. `ConsolidationCandidate: Codable, Identifiable` -- fields: driveId (Int), driveName (String), totalFiles (Int), totalSizeBytes (Int64), uniqueFiles (Int), uniqueSizeBytes (Int64), duplicatedFiles (Int), duplicatedSizeBytes (Int64), reclaimableBytes (Int64), isCandidate (Bool), totalAvailableSpace (Int64), targetDrives ([ConsolidationTargetDrive]). CodingKeys for snake_case. Computed `id` returning `driveId`.

5. `ConsolidationCandidatesResponse: Codable` -- fields: candidates ([ConsolidationCandidate]), totalDrives (Int), consolidatableCount (Int). CodingKeys for snake_case.

6. `StrategyFile: Codable, Identifiable` -- fields: path (String), sizeBytes (Int64), partialHash (String?). CodingKeys for snake_case. Computed `id` returning `path`.

7. `StrategyAssignment: Codable, Identifiable` -- fields: targetDrive (String), fileCount (Int), totalBytes (Int64), files ([StrategyFile]). CodingKeys for snake_case. Computed `id` returning `targetDrive`.

8. `StrategyTargetDrive: Codable, Identifiable` -- fields: driveName (String), capacityBytes (Int64), freeBefore (Int64), freeAfter (Int64). CodingKeys for snake_case. Computed `id` returning `driveName`.

9. `ConsolidationStrategyResponse: Codable` -- fields: sourceDrive (String), totalUniqueFiles (Int), totalUniqueBytes (Int64), totalBytesToTransfer (Int64), isFeasible (Bool), assignments ([StrategyAssignment]), unplaceable ([StrategyFile]), targetDrives ([StrategyTargetDrive]). CodingKeys for snake_case.

**Migration.swift** -- mirror `src/drivecatalog/api/models/migration.py`:

1. `GeneratePlanRequest: Codable` -- fields: sourceDrive (String). CodingKeys: source_drive.

2. `MigrationFileResponse: Codable, Identifiable` -- fields: id (Int), sourcePath (String), sourceSizeBytes (Int64), targetDriveName (String?), targetPath (String?), action (String), status (String), error (String?). CodingKeys for snake_case.

3. `FileStatusCount: Codable` -- fields: count (Int), bytes (Int64).

4. `MigrationPlanSummary: Codable, Identifiable` -- fields: planId (Int), sourceDrive (String), status (String), totalFiles (Int), filesToCopy (Int), filesToDelete (Int), totalBytesToTransfer (Int64), isFeasible (Bool). CodingKeys for snake_case. Computed `id` returning `planId`.

5. `MigrationPlanResponse: Codable, Identifiable` -- fields: planId (Int), sourceDriveName (String), status (String), totalFiles (Int), filesToCopy (Int), filesToDelete (Int), totalBytesToTransfer (Int64), filesCompleted (Int), bytesTransferred (Int64), filesFailed (Int), errors ([String]), operationId (String?), createdAt (String), startedAt (String?), completedAt (String?), fileStatusCounts ([String: FileStatusCount]). CodingKeys for snake_case. Computed `id` returning `planId`.

   NOTE: Use String (not Date) for createdAt/startedAt/completedAt because the migration API returns plain datetime strings. This avoids date parsing issues -- the UI can display these as-is or parse them locally.

6. `TargetSpaceInfo: Codable, Identifiable` -- fields: driveName (String), bytesNeeded (Int64), bytesAvailable (Int64), sufficient (Bool). CodingKeys for snake_case. Computed `id` returning `driveName`.

7. `ValidatePlanResponse: Codable` -- fields: planId (Int), status (String), valid (Bool), targetSpace ([TargetSpaceInfo]). CodingKeys for snake_case.

8. `MigrationFilesResponse: Codable` -- fields: planId (Int), files ([MigrationFileResponse]), total (Int). CodingKeys for snake_case.

9. `ExecuteResponse: Codable` -- fields: planId (Int), operationId (String), status (String), pollUrl (String). CodingKeys for snake_case.

**Patterns to follow (from Drive.swift, Operation.swift):**
- Use `Int64` for all byte-count fields (matching Python int capacity)
- Every struct with snake_case API keys gets explicit CodingKeys enum
- Add `Identifiable` conformance to models used in SwiftUI lists
- Use computed `var id` property when natural id differs from `id` field name
- Import Foundation at top of each file
  </action>
  <verify>
Build the Xcode project: `cd /Users/timkalkkuhl/code/DriveSnapshots && xcodegen generate && xcodebuild -scheme DriveCatalog -configuration Debug build 2>&1 | tail -5`
The project should compile with zero errors. All new types should be visible to other files in the target.
  </verify>
  <done>
Consolidation.swift contains 9 Codable structs matching all consolidation Pydantic models.
Migration.swift contains 9 Codable structs matching all migration Pydantic models.
Project compiles successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: APIService Consolidation and Migration Methods</name>
  <files>
    DriveCatalog/Services/APIService.swift
  </files>
  <action>
Extend the existing APIService actor with methods for all consolidation and migration endpoints. Add two new MARK sections after the existing ones.

**MARK: - Consolidation Endpoints** (3 methods):

1. `func fetchDistribution() async throws -> DriveDistributionResponse`
   - GET /consolidation/distribution
   - No parameters
   - Uses existing `get(url:)` helper

2. `func fetchConsolidationCandidates() async throws -> ConsolidationCandidatesResponse`
   - GET /consolidation/candidates
   - No parameters
   - Uses existing `get(url:)` helper

3. `func fetchConsolidationStrategy(drive: String) async throws -> ConsolidationStrategyResponse`
   - GET /consolidation/strategy?drive={drive}
   - Pass drive name as query parameter
   - Uses existing `get(url:)` helper with queryItems

**MARK: - Migration Endpoints** (6 methods):

4. `func generateMigrationPlan(sourceDrive: String) async throws -> MigrationPlanSummary`
   - POST /migrations/generate
   - Body: GeneratePlanRequest(sourceDrive: sourceDrive)
   - Uses existing `post(url:body:)` helper

5. `func fetchMigrationPlan(planId: Int) async throws -> MigrationPlanResponse`
   - GET /migrations/{planId}
   - Uses existing `get(url:)` helper

6. `func validateMigrationPlan(planId: Int) async throws -> ValidatePlanResponse`
   - POST /migrations/{planId}/validate
   - No body -- uses existing `postEmpty(url:)` helper

7. `func executeMigrationPlan(planId: Int) async throws -> ExecuteResponse`
   - POST /migrations/{planId}/execute
   - No body -- uses existing `postEmpty(url:)` helper

8. `func fetchMigrationFiles(planId: Int, status: String? = nil, limit: Int = 100, offset: Int = 0) async throws -> MigrationFilesResponse`
   - GET /migrations/{planId}/files?status={status}&limit={limit}&offset={offset}
   - status is optional query parameter (only add if non-nil)
   - Uses existing `get(url:)` helper with queryItems

9. `func cancelMigration(planId: Int) async throws`
   - DELETE /migrations/{planId}
   - Uses existing `delete(url:)` helper

**Follow existing APIService patterns exactly:**
- Use `buildURL(path:queryItems:)` for URL construction
- Use the generic `get()`, `post()`, `postEmpty()`, `delete()` helpers
- Match doc comment style of existing methods (/// with - Parameters/Returns where appropriate)
- Optional query parameters: only append URLQueryItem when value is non-nil
  </action>
  <verify>
Build the Xcode project: `cd /Users/timkalkkuhl/code/DriveSnapshots && xcodebuild -scheme DriveCatalog -configuration Debug build 2>&1 | tail -5`
The project should compile with zero errors. All 9 new methods should be accessible from any Swift file in the project.
  </verify>
  <done>
APIService has 9 new async methods covering all consolidation (3) and migration (6) endpoints.
Each method correctly maps to the backend API path, HTTP method, and request/response types.
Project compiles successfully.
  </done>
</task>

</tasks>

<verification>
1. `xcodegen generate` succeeds (new files picked up)
2. `xcodebuild -scheme DriveCatalog -configuration Debug build` succeeds with zero errors
3. Consolidation.swift has 9 structs matching Python consolidation models
4. Migration.swift has 9 structs matching Python migration models
5. APIService.swift has 9 new methods covering all consolidation and migration endpoints
</verification>

<success_criteria>
- All consolidation and migration Pydantic models have corresponding Swift Codable structs
- All API endpoints have corresponding APIService async methods
- Xcode project compiles cleanly
- Types are ready for the wizard view to consume in Plan 23-02
</success_criteria>

<output>
After completion, create `.planning/phases/23-migration-wizard-ui/23-01-SUMMARY.md`
</output>
