import SwiftUI

struct DuplicatesView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Duplicate detection coming in Phase 17")
                .foregroundStyle(.secondary)
            Spacer()
        }
        .navigationTitle("Duplicates")
    }
}

#Preview {
    DuplicatesView()
}
