import SwiftUI

struct SearchView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Search interface coming in Phase 18")
                .foregroundStyle(.secondary)
            Spacer()
        }
        .navigationTitle("Search")
    }
}

#Preview {
    SearchView()
}
